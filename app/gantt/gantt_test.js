'use strict';

describe('gantt module', function() {

  beforeEach(module('gantt'));

  describe('gantt controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ganttCtrl = $controller('GanttCtrl');
      expect(ganttCtrl).toBeDefined();
    }));

  });
});